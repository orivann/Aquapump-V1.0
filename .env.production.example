# ===============================================
# AquaPump Production Environment Configuration
# ===============================================
# Copy this file to .env.production and fill in real values before deploying

# Frontend Variables (accessible in client-side code via EXPO_PUBLIC_ prefix)
EXPO_PUBLIC_RORK_API_BASE_URL=https://aquapump.example.com
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
EXPO_PUBLIC_TOOLKIT_URL=https://toolkit.rork.com

# Backend Variables (server-side only, never exposed to client)
NODE_ENV=production
PORT=8081
CORS_ORIGIN=https://aquapump.example.com

# Supabase Backend Service Key (NEVER expose to client!)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# Logging
LOG_LEVEL=warn

# Notes:
# - Keep .env.production out of version control
# - Use secrets manager or Kubernetes secrets for production
# - For Docker Compose production: docker-compose -f infra/docker-compose.prod.yml up -d
