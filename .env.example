# ===============================================
# AquaPump Environment Configuration
# ===============================================

# Frontend Variables (accessible in client-side code)
# These are embedded in the build and visible to users
EXPO_PUBLIC_API_URL=http://localhost:8081/api
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
EXPO_PUBLIC_TOOLKIT_URL=https://toolkit.rork.com
EXPO_PUBLIC_AI_CHAT_KEY=your-ai-chat-key

# Backend Variables (server-side only, never exposed to client)
# Keep these secure!
NODE_ENV=development
PORT=8081
CORS_ORIGIN=*

# Supabase Backend (Service Key - NEVER expose to client!)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# Logging
LOG_LEVEL=info

# ===============================================
# Production Configuration
# ===============================================
# For production, use Kubernetes secrets instead!
# kubectl create secret generic aquapump-secrets \
#   --from-literal=SUPABASE_URL=... \
#   --from-literal=SUPABASE_SERVICE_KEY=... \
#   -n aquapump-production

# ===============================================
# Optional Services (Future)
# ===============================================
# Redis (for caching)
# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=your-redis-password

# Monitoring
# SENTRY_DSN=https://your-sentry-dsn
# NEW_RELIC_LICENSE_KEY=your-new-relic-key

# Email (for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-email-password
